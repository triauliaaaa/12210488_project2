import 'package:flutter/material.dart';

class PengaturanPanel extends StatelessWidget {
  const PengaturanPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Pengaturan'),
    );
  }
}
